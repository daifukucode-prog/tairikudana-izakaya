# ============================================
# 大陸棚居酒屋 — 環境変数テンプレート
# ============================================
# このファイルをコピーして .env.local を作成してください:
#   cp .env.example .env.local
#
# ⚠️ .env.local は .gitignore で除外されており、Gitにはコミットされません。

# ----- Supabase -----
# Supabase プロジェクトの URL と Anonymous Key
# Supabase ダッシュボード > Settings > API から取得できます
NEXT_PUBLIC_SUPABASE_URL=your_supabase_project_url
NEXT_PUBLIC_SUPABASE_ANON_KEY=your_supabase_anon_key

# ----- 管理画面認証 -----
# 管理画面にログインするためのユーザー名とパスワード
ADMIN_USER=your_admin_username
ADMIN_PASSWORD=your_secure_password

# JWT トークンの署名に使う秘密鍵（必須）
# 以下のコマンドで生成できます:
#   openssl rand -base64 32
#
# ⚠️ この値を設定しないと、サーバー再起動やVercelのサーバーレス環境で
#    リクエストごとに秘密鍵が変わり、ログイン状態が維持できなくなります。
AUTH_SECRET=your_random_secret_key
